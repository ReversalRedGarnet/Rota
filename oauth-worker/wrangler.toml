name = "rotaract-honiara-decap-oauth"
main = "src/index.js"
compatibility_date = "2026-09-24"

# GITHUB_OAUTH_ID, GITHUB_OAUTH_SECRET (required) and GITHUB_REPO_PRIVATE
# (optional) are set as secrets after deploying, not written here — see
# README.md. Never commit real client secrets to this file or the repo.
