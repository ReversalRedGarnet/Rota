{
  "name": "rotaract-honiara-decap-oauth",
  "private": true,
  "description": "Decap CMS login endpoint (Cloudflare Access email login) so board members can edit the site's content without a GitHub account.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
